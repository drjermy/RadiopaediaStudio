appId: com.radiopaedia.pacs-anonymizer
productName: PACS Anonymizer
directories:
  output: build
  buildResources: build-resources
files:
  - dist/**/*
  - src/renderer/**/*
  - package.json
extraResources:
  - from: backend/dist/pacs-anonymizer-backend
    to: backend-bin
    filter:
      - "**/*"
mac:
  target:
    - target: dmg
      arch:
        - arm64
  category: public.app-category.medical
  identity: null
